apiVersion: apps/v1
kind: Deployment
metadata:
  name: telecom-api
  namespace: telecom
spec:
  replicas: 2
  selector:
    matchLabels:
      app: telecom-api
  template:
    metadata:
      labels:
        app: telecom-api
    spec:
      containers:
        - name: telecom-api
          image: ${IMAGE}
          imagePullPolicy: Always
          ports:
            - containerPort: 5000
          env:
            - name: CUSTOMER_DATA
              value: "/data/customers.csv"